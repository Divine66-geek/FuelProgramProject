<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.layout.VBox?>
<?import javafx.scene.layout.HBox?>
<?import javafx.scene.control.Label?>
<?import javafx.scene.control.Button?>
<?import javafx.scene.control.Separator?>
<?import javafx.geometry.Insets?>

<VBox xmlns:fx="http://javafx.com/fxml"
      fx:controller="application.PaymentController"
      spacing="20" alignment="CENTER"
      style="-fx-padding: 30; -fx-background-color: black; -fx-border-radius: 10; -fx-background-radius: 10;">

    <!-- Title -->
    <Label text="⛽ Fuel Station Payment"
           style="-fx-font-size: 22px; -fx-font-weight: bold; -fx-text-fill: #1abc9c;" />

    <Separator prefWidth="300" />

    <!-- Car Details -->
    <VBox spacing="10" alignment="CENTER_LEFT" prefWidth="300">
        <Label text="🚗 Vehicle Details"
               style="-fx-font-size: 16px; -fx-font-weight: bold; -fx-text-fill: #1abc9c;" />
        <Label fx:id="carDetailsLabel"
               wrapText="true"
               style="-fx-font-size: 14px; -fx-text-fill: #1abc9c;" />
    </VBox>

    <!-- Payment Amount -->
    <VBox spacing="10" alignment="CENTER_LEFT" prefWidth="300">
        <Label text="💸 Amount Due"
               style="-fx-font-size: 16px; -fx-font-weight: bold; -fx-text-fill: #1abc9c;" />
        <Label fx:id="amountLabel"
               style="-fx-font-size: 18px; -fx-text-fill: #2e7d32; -fx-font-weight: bold;" />
    </VBox>

    <!-- Confirm Button -->
    <Button text="✅ Confirm Payment"
            onAction="#confirmPayment"
            style="-fx-background-color: #4CAF50; -fx-text-fill: white; -fx-font-size: 16px; -fx-padding: 10 30 10 30; -fx-background-radius: 5;" />

</VBox>
